[package]
name = "led-detector"
version = "0.1.0"
edition = "2024"

[dependencies]
image = "0.25"
rayon = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "led_benchmark"
harness = false
